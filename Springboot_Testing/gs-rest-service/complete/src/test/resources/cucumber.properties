# specify the directory where your feature files are located
features = src/test/resources

# specify the package where your step definitions are located
glue = com.example.restservice.steps

# specify desired plugins (e.g., pretty, html)
plugin = pretty, html:target/cucumber-reports
